# User

Operations about user

## Create user

`POST /user`

```@raw html
<OAOperation operationId="createUser" prefix-headings="true" />
```

## Creates list of users with given input array

`POST /user/createWithArray`

```@raw html
<OAOperation operationId="createUsersWithArrayInput" prefix-headings="true" />
```

## Creates list of users with given input array

`POST /user/createWithList`

```@raw html
<OAOperation operationId="createUsersWithListInput" prefix-headings="true" />
```

## Logs user into the system

`GET /user/login`

```@raw html
<OAOperation operationId="loginUser" prefix-headings="true" />
```

## Logs out current logged in user session

`GET /user/logout`

```@raw html
<OAOperation operationId="logoutUser" prefix-headings="true" />
```

## Delete user

`DELETE /user/{username}`

```@raw html
<OAOperation operationId="deleteUser" prefix-headings="true" />
```

## Get user by user name

`GET /user/{username}`

```@raw html
<OAOperation operationId="getUserByName" prefix-headings="true" />
```

## Updated user

`PUT /user/{username}`

```@raw html
<OAOperation operationId="updateUser" prefix-headings="true" />
```

